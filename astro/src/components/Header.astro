---
import { SITE_TITLE } from '../consts';
import ThemeToggle from './ThemeToggle.astro';
---

<header>
	<nav>
		<a class="marque" href="/">
			<span class="chevron">›</span>{SITE_TITLE}<span class="curseur" aria-hidden="true"></span>
		</a>
		<ThemeToggle />
	</nav>
</header>

<style>
	header {
		padding: 1rem 1.5rem;
		border-bottom: 1px solid var(--border, #331c14);
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}
	.marque {
		font-family: var(--font-mono, monospace);
		font-weight: 800;
		text-decoration: none;
		color: inherit;
	}
	.chevron {
		opacity: 0.6;
		margin-right: 0.25em;
	}
	.curseur {
		display: inline-block;
		width: 0.5em;
		height: 1.5em;
		margin-left: 0.15em;
		background: currentColor;
		vertical-align: text-bottom;
		animation: clignotement-curseur 1s step-end infinite;
	}

	@keyframes clignotement-curseur {
		0% {
			opacity: 1;
		}
		50% {
			opacity: 0;
		}
		100% {
			opacity: 1;
		}
	}
</style>
