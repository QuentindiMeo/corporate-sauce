---
// Pastille signature (charte §5) : « QDM » sur aplat --acc, mono 800, radius 8px.
---

<span class="pastille-qdm">QDM</span>

<style>
	.pastille-qdm {
		display: inline-block;
		font-family: var(--font-mono, monospace);
		font-weight: 800;
		font-size: 19px;
		padding: 0.1em 0.45em;
		border-radius: 8px;
		background: var(--acc, var(--cool, currentColor));
		color: var(--acc-ink, var(--ink, #120a07));
	}
</style>
